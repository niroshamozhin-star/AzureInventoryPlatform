@model Warehouse
@{
    ViewData["Title"] = "Delete Warehouse";
}

<h1>Delete Warehouse</h1>

<div class="alert alert-warning">Are you sure you want to delete this warehouse?</div>

<dl class="row col-md-6">
    <dt class="col-sm-3">Warehouse Code</dt>
    <dd class="col-sm-9">@Model.WarehouseCode</dd>
    <dt class="col-sm-3">Name</dt>
    <dd class="col-sm-9">@Model.WarehouseName</dd>
    <dt class="col-sm-3">City</dt>
    <dd class="col-sm-9">@Model.City</dd>
</dl>

<form asp-action="Delete" asp-route-id="@Model.Id">
    <button type="submit" class="btn btn-danger">Delete</button>
    <a asp-action="Index" class="btn btn-link">Cancel</a>
</form>
